<?xml version="1.0" encoding="utf-8"?>

<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:orientation="vertical" android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:weightSum="1"
    android:alignmentMode="alignMargins">

    <TextView android:id="@+id/habit_item_text_title"
        android:layout_width="289dp"
        android:layout_height="30dp"
        android:gravity="top"
        android:textStyle="bold"
        android:textSize="22sp"
        android:textColor="#FFFFFF"
        android:layout_alignParentTop="true"
        android:layout_alignParentStart="true" />

    <ImageView
        android:layout_width="54dp"
        android:layout_height="0dp"
        android:id="@+id/habit_item_image"
        android:layout_gravity="right|top"
        android:layout_toRightOf="@+id/habit_item_text_title"
        android:layout_alignParentEnd="true"
        android:layout_alignParentTop="true"
        android:layout_alignBottom="@+id/habit_item_text_description"
        android:layout_alignTop="@id/habit_item_text_title"
        android:paddingTop="5dp"
        android:paddingRight="5dp"
        android:paddingLeft="5dp"
        android:paddingBottom="5dp"
        />

    <TextView
        android:layout_width="288dp"
        android:layout_height="20dp"
        android:textStyle="normal"
        android:textSize="14sp"
        android:textColor="#FFF"
        android:id="@+id/habit_item_text_description"
        android:layout_below="@+id/habit_item_text_title"
        android:layout_alignEnd="@+id/habit_item_text_title"
        style="@style/AppTheme" />

</RelativeLayout>